# Shell scripts must stay LF: a CRLF script silently fails under bash — its shebang runs it under
# bash, and `\r` breaks the first construct (observed on .devcontainer/post-create.sh, D049).
*.sh text eol=lf
